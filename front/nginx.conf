server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # 重要：まず実ファイル（blogなど）を探し、なければSPA(index.html)を返す
        try_files $uri $uri/ /index.html;
    }
}